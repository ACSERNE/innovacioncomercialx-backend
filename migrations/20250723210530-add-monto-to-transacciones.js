'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // No hace nada, solo marca migración aplicada
  },

  async down(queryInterface, Sequelize) {
    // Revertir vacío
  }
};
 